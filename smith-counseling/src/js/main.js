class Node {
  constructor(key, left, right, parent) {
    this.key = parseInt(key);
    this.left = left;
    this.right = right;
    this.parent = parent;
  }
}
// eslint-disable-next-line
const NODE_1 = new Node();
